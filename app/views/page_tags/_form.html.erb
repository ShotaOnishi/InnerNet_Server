<%= form_for(@page_tag) do |f| %>
  <% if @page_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_tag.errors.count, "error") %> prohibited this page_tag from being saved:</h2>

      <ul>
      <% @page_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :page_id %><br>
    <%= f.text_field :page_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.text_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
